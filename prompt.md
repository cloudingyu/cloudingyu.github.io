# 课堂笔记批量转写任务

请只处理当前项目中的 `_notes/**/*.md` 文件，不要修改其他文件。

## 目标

把所有课堂笔记整理成适合公开网站阅读的知识型笔记，而不是课堂转述稿。

需要完成两件事：

1. 精简每篇笔记 front matter 里的 `subtitle`
2. 改写正文，去掉老师口吻、课堂闲聊、课程安排和作业提醒，只保留知识点、例子、公式、推导、定义、算法、表格、代码等学习内容

## front matter 规则

每篇文件开头都有 YAML front matter。请保留已有字段，不要删除：

- `layout`
- `title`
- `subtitle`
- `course`
- `course_description`
- `date`
- `header-img`
- `header-mask`
- `mathjax`

只改 `subtitle` 的内容。

### subtitle 改写规则

当前 `subtitle` 往往是大量知识点并列，读起来很困难。请改成一句更概括、更像文章摘要的短句。

要求：

- 用中文
- 长度建议 18 到 36 个汉字左右
- 不要堆砌超过 3 个并列知识点
- 不要写成完整目录
- 不要使用“本节课讲了”“老师介绍了”等课堂口吻
- 应概括这篇笔记的核心主题或主线

示例：

```yaml
subtitle: 'RAID存储系统、磁盘空间管理、缓冲区管理、文件组织方式、代价模型、表空间、索引基础、ISAM索引、B+树索引'
```

可以改为：

```yaml
subtitle: '从磁盘组织到索引结构的数据库存储基础'
```

## 正文改写规则

### 必须保留

请保留所有真正的知识内容，包括但不限于：

- 定义
- 定理
- 性质
- 公式
- 推导
- 算法步骤
- 模型结构
- 例子
- 反例
- 表格
- 代码块
- 图示说明
- 课堂中用于解释知识点的案例

不要为了“精简”而删掉知识点。正文可以变得更清晰，但不要明显变短。

### 必须删除或改写

删除明显无关的内容：

- 课间闲聊
- 课程安排
- 调课、补课、节假日进度说明
- 作业提醒
- 考试提醒
- “下节课再讲”“后面会讲”
- “今天这节课很关键”这类课堂推进语

改写老师口吻：

- `老师强调：...`
- `老师指出：...`
- `老师提醒大家：...`
- `老师总结：...`
- `老师解释：...`
- `老师举例：...`
- `老师预告：...`
- `老师说：...`

这些内容如果包含知识点，不要删除，而是改写为中性的知识表述。

例如：

```markdown
> **老师强调**：数据不是先把一块盘填满再去填另一块，而是分散开来，同时存在多块硬盘上，这样才能同时去读。
```

应改为：

```markdown
RAID 的条带化并不是先填满一块磁盘再写入下一块，而是把数据分散到多块磁盘上，从而支持并行读取。
```

再例如：

```markdown
老师先提醒大家：本学期节假日会冲掉几次课，因此需要加快进度。今天这节课非常关键，因为它连接了传统统计序列模型和后面要讲的深度学习序列模型。
```

应改为：

```markdown
序列建模是传统统计模型与深度学习序列模型之间的重要过渡主题。
```

## 风格要求

- 使用中性、知识型、教材式或技术博客式表述
- 不要出现“老师”“课堂上”“同学们”“我们下节课”“今天这节课”等课堂叙述视角
- 可以保留“例如”“典型场景”“核心思想”等知识组织词
- 保持原有 Markdown 层级结构，除非原结构明显不合理
- 保持数学公式、代码块、表格的 Markdown 格式
- 不要把中文全角标点改乱
- 不要新增英文解释，除非原文已有英文术语

## 验收标准

完成后请检查：

1. `_notes/**/*.md` 中不应再出现明显老师口吻：
   - `老师强调`
   - `老师指出`
   - `老师提醒`
   - `老师总结`
   - `老师解释`
   - `老师举例`
   - `老师预告`
   - `老师说`
2. `_notes/**/*.md` 中不应再出现明显课程安排、作业、调课、补课等无关内容。
3. 每篇 `subtitle` 应是概括句，而不是长串知识点清单。
4. 运行 `bundle exec jekyll build`，确保构建通过。

请直接修改文件。完成后简要汇报处理数量、主要改动和构建结果。
